# Legacy migrations (PocketMenu / food)

Миграции **001–051** от ресторанной платформы (меню, заказы, iiko, Quick Resto, доставка).

**Не применять** на новой базе INUU. Актуальная цепочка: `../migrations/001_inuu_*.sql` … `019_inuu_seed_ulan_ude.sql`.

Если нужно поднять старую БД с историей — временно верните файлы в `../migrations/` (не смешивать с INUU на одной БД без ручной миграции данных).
